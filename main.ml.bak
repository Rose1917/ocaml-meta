(* define the boost type. by default it will not boost*)
open Util.Mnist
open Op.Op_base
let _ = 
  let _test_set,_,test_label = load_test_data() in
  for it = 0 to 10000 - 1 do
        let label = reshape (Bigarray.Genarray.slice_left test_label [|it|]) [|1;10|] in
        print label;
  done
  (*print train_set ~prefix:"train:set";;*)
